pipeline { 

  environment  { 
        RESGISTRY = "docker.io/username"
        IMAGE_NAME = "app-1"
}

stages   {

  stage(checkout code) {

   steps  {

    git branch: master


 stage("Build Docker Image") {
     }

  stage (Login to Registry)
}
 stage (push image) 

} 
